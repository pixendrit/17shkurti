# Hijeshi dashboard — design

Built with the design recipe from *How to Design Programs*: information is
represented by data definitions first, and every function follows from its
data — signature, purpose statement, examples (as tests), template, body.

## 1. Architecture

```
routes/            thin: parse input → call a command/query → render
lib/server/        the imperative shell: D1 access, one batch per request
  repo.ts          loadWorld(db): World          — ONE read round trip
                   commit(db, changes): void     — ONE atomic write batch
lib/domain/        the functional core: pure data + functions, no I/O
  *.ts             data definitions and the functions over them
  *.test.ts        the examples, as tests
```

Every request is **world → decision → changes**:

1. `loadWorld` reads everything the shop has in one batched query. The whole
   shop is a few megabytes, so reading it at once is cheaper than any number
   of small queries: each query is a network round trip.
2. A pure command `(world, input, now) → Result<Change[]>` decides what
   happens. It never touches the database, so it's tested with plain data.
3. `commit` applies the changes in one D1 batch, which is a transaction:
   everything is written or nothing is.

Ids are generated by the domain (`crypto.randomUUID()`), so a command can
create an order, its lines, its customer and its stock movements together,
and they all land in the same batch.

The database lives in the Western Europe region (the shop is in Kosovo).

## 2. Data definitions

### Money
```
Cents = integer            // an amount of euro, in cents: 2450 is 24,50 €
```
All money is integer cents: in the database, in the domain, in arithmetic.
Only the edges convert (parsing "24,50", formatting "24,50 €").

### Catalogue
```
Garment = 'oversized_200g' | 'regular_fit'
Color   = 'black' | 'white'
Size    = 'XS' | 'S' | 'M' | 'L' | 'XL' | 'XXL'
Sku     = { garment: Garment, color: Color, size: Size }
          // one kind of blank shirt, e.g. an oversized black M
Design  = { id, name, archived: boolean }
          // an artwork, e.g. "UÇK"
Print   = { id, designId, shirtColor: Color, perSheet: integer ≥ 1,
            front?: ImageId, back?: ImageId }
          // the DTF version of a design for one shirt colour: the print
          // differs between black and white shirts. perSheet is how many
          // shirts' worth (front + back) fit on one 56 × 100 cm sheet.
```
An order line names a design and a shirt colour; the print follows from them.

### Artwork (itemization)
```
Artwork =
  | { kind: 'none' }                                   // plain shirt
  | { kind: 'print', printId }                         // a catalogue design
  | { kind: 'custom', front: ImageId, back: ImageId,   // personalised: its
      printReady: boolean }                            // own DTF, with mockups
```
A custom line can't exist without both mockups: the type has no way to say it.

### Customer
```
Customer = { id, name, phone, address?, city?, country: Country }
Country  = 'XK' | 'AL' | 'MK' | 'OTHER'
```

### Order
```
OrderKind = 'sale' | 'gift'            // gift: sent free to an influencer
Channel   = 'instagram' | 'messenger' | 'tiktok' | 'whatsapp' | 'direct' | 'other'
Delivery  =
  | { method: 'courier', cost: Cents, trackingRef?: string }
  | { method: 'hand',    cost: Cents }
          // cost: what the SHOP pays to deliver it. Shipping is free for
          // the customer unless shippingCharged says otherwise.
LineCost  = { blank: Cents, dtf: Cents, labor: Cents }
          // per shirt, snapshotted when the order is created, so a later
          // price change never rewrites past profit
OrderLine = { id, sku: Sku, artwork: Artwork, quantity: integer ≥ 1,
              unitPrice: Cents, cost: LineCost }
Order     = { id, code: 'HS-0042', customerId, kind, channel, delivery,
              lines: OrderLine[] (≥ 1), packaging: Cents,
              shippingCharged: Cents, discount: Cents, notes?,
              status: Status, stockTracked: boolean, isDemo: boolean,
              createdAt, madeAt?, handedOverAt?, deliveredAt?, returnedAt?,
              cancelledAt? }
          // stockTracked is false for history imported from before the app,
          // whose shirts were never in the app's stock
```

### Status and events (the order process)
```
Status = 'new' | 'in_production' | 'ready' | 'with_courier'
       | 'delivered' | 'returned' | 'cancelled'
Event  = 'start' | 'make' | 'hand_over' | 'deliver' | 'return' | 'cancel'
```
| from \ event   | start | make  | hand_over (courier) | deliver           | return  | cancel |
|----------------|-------|-------|---------------------|-------------------|---------|--------|
| new            | in_production | ready | | | | cancelled |
| in_production  |       | ready |                     |                   |         | cancelled |
| ready          |       |       | with_courier        | delivered (hand)  |         | cancelled |
| with_courier   |       |       |                     | delivered         | returned |       |

`make` is the only way into `ready`, and it is what takes the order's
blanks and prints out of stock, and only if they are there.

### Payments
```
PaymentMethod = 'cod' | 'bank' | 'cash'
Payment = { id, orderId, amount: Cents, method, receivedAt }
```
An order is paid when its payments cover what it's owed. The courier's
settlement is one payment per parcel.

### Stock (a ledger)
```
Subject  = { kind: 'blank', sku } | { kind: 'print', printId }
Movement = { id, subject, delta: integer, reason, orderId?, purchaseId?, at }
Reason   = 'opening' | 'purchase' | 'made' | 'adjustment'
```
What's on the shelf is the sum of a subject's movements. Nothing stores a
count that could drift from its history.

**Allocation.** Open, unmade orders are served in the order they arrived:
the oldest order gets stock first. So two orders can never both be "can make"
with the last shirt.

### Purchases (money out)
```
Purchase =
  | { kind: 'blanks', lines: [{ sku, quantity, unitCost: Cents }] }
          // adds the blanks to stock
  | { kind: 'dtf', sheets, sheetPrice: Cents, prints: [{ printId, quantity }] }
          // adds the prints to stock
  | { kind: 'expense', category: 'packaging' | 'marketing' | 'other',
      description, amount: Cents }
  (each with id, date, isDemo)
```
A blank's cost is the weighted average of what was paid for it; until it has
been bought through the app, the default from Settings.

### Settings
```
Settings = { defaultPrice, sheetPrice, customPerSheet, laborPerShirt,
             packagingPerOrder: Cents-or-count,
             blankCost: Garment → Cents, courierCost: Country → Cents }
```

## 3. Database

Tables mirror the data definitions; `CHECK` constraints enforce what the
types say (quantities ≥ 1, artwork columns consistent with the artwork kind,
exactly one subject per movement). All money columns are `*_cents INTEGER`.

customers · designs · prints · images · orders · order_lines · payments ·
purchases · purchase_lines · stock_movements · settings · login_attempts

Demo data is flagged on orders and purchases; clearing it deletes them with
their lines, payments and movements, leaving real records untouched.
